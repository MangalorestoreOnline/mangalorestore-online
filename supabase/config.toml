project_id = "mangalorestore"

[api]
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322

[auth]
site_url = "http://localhost:3000"
additional_redirect_urls = ["https://mangalorestore.online", "https://*.vercel.app"]
jwt_expiry = 3600
enable_signup = true

[storage]
file_size_limit = "50MiB"
